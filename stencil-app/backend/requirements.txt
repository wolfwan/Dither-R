# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# SVG parsing
svgelements==1.9.6

# 2D geometry (polygon containment, boolean ops, nearest-point queries)
shapely==2.0.6

# 3D geometry (extrusion, boolean union, STL export)
trimesh==4.4.9
manifold3d==2.5.1
numpy==1.26.4
mapbox_earcut==2.0.0  # polygon-with-holes triangulation for extrude_polygon
scipy==1.17.1  # trimesh mesh-repair utilities (face graph, watertight checks)
networkx==3.6.1  # trimesh mesh-repair utilities

# Dev / testing
pytest==8.3.3
httpx==0.27.2
